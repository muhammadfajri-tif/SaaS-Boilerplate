# =============================================================================
# Docker Production Environment Variables
# =============================================================================
# This file is a TEMPLATE for production environment variables
# DO NOT commit actual production secrets to version control!
# 
# Usage:
#   1. Copy this file to `.env.production` on your production server
#   2. Update ALL values with actual production credentials
#   3. Ensure proper file permissions: chmod 600 .env.production
#   4. Run: docker-compose -f docker/docker-compose.prod.yml up -d
# =============================================================================

# -----------------------------------------------------------------------------
# Container Registry Configuration
# -----------------------------------------------------------------------------
# GitHub Container Registry example:
# CONTAINER_REGISTRY=ghcr.io/yourusername/yourrepo
# 
# Azure Container Registry example:
# CONTAINER_REGISTRY=yourregistry.azurecr.io
# 
# Docker Hub example:
# CONTAINER_REGISTRY=docker.io/yourusername
CONTAINER_REGISTRY=ghcr.io/yourusername/yourrepo
IMAGE_TAG=latest

# -----------------------------------------------------------------------------
# PostgreSQL Configuration (CHANGE THESE!)
# -----------------------------------------------------------------------------
POSTGRES_USER=prod_admin
POSTGRES_PASSWORD=CHANGE_THIS_STRONG_PASSWORD_12345
POSTGRES_DB=saas_production
POSTGRES_PORT=5432

# -----------------------------------------------------------------------------
# Backend Configuration
# -----------------------------------------------------------------------------
BACKEND_PORT=3001
NODE_ENV=production

# Database URL (for backend to connect to PostgreSQL)
# Use 'postgres' as host when running inside Docker
DATABASE_URL=postgresql://prod_admin:CHANGE_THIS_STRONG_PASSWORD_12345@postgres:5432/saas_production

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------
# Add your production Vercel domain(s)
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,https://your-vercel-app.vercel.app

# -----------------------------------------------------------------------------
# Authentication (REQUIRED - Get from Clerk dashboard)
# -----------------------------------------------------------------------------
JWT_SECRET=CHANGE_THIS_TO_RANDOM_64_CHARACTER_STRING
CLERK_SECRET_KEY=sk_live_your_actual_clerk_secret_key
CLERK_PUBLISHABLE_KEY=pk_live_your_actual_clerk_publishable_key

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
API_KEY=CHANGE_THIS_TO_RANDOM_API_KEY

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
LOG_LEVEL=info

# -----------------------------------------------------------------------------
# Data Storage Path (for volume mounting)
# -----------------------------------------------------------------------------
# Absolute path where PostgreSQL data will be stored
DATA_PATH=/var/lib/saas/data/postgres

# -----------------------------------------------------------------------------
# NGINX Configuration (if using NGINX profile)
# -----------------------------------------------------------------------------
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443
DOMAIN_NAME=api.yourdomain.com

# -----------------------------------------------------------------------------
# Optional: Monitoring & Observability
# -----------------------------------------------------------------------------
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
# LOGTAIL_SOURCE_TOKEN=your-logtail-token

# -----------------------------------------------------------------------------
# Optional: Azure-specific (if deploying to Azure)
# -----------------------------------------------------------------------------
# AZURE_STORAGE_CONNECTION_STRING=your-connection-string
# AZURE_KEY_VAULT_URI=https://your-vault.vault.azure.net/

# =============================================================================
# IMPORTANT SECURITY NOTES:
# =============================================================================
# 1. NEVER commit this file with real credentials to git
# 2. Use strong, randomly generated passwords (minimum 32 characters)
# 3. Rotate secrets regularly
# 4. Use Azure Key Vault or similar for sensitive data in production
# 5. Set proper file permissions: chmod 600 .env.production
# 6. Consider using Docker secrets for additional security
# =============================================================================
