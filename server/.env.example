# =============================================================================
# Backend Server Environment Variables
# =============================================================================
# This file contains all environment variables needed for the Express backend
# 
# Usage:
#   - For local development: Copy to `.env` and update values
#   - For Docker: Use `.env.docker.dev` or `.env.docker.prod` instead
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL Database Configuration
# -----------------------------------------------------------------------------
# PostgreSQL credentials
# POSTGRES_USER=user
# POSTGRES_PASSWORD=password
# POSTGRES_DB=saas
# POSTGRES_PORT=5432

# Database URL
# For local development (backend runs on host):
DATABASE_URL=postgresql://user:password@localhost:5432/saas

# For Docker (backend runs in container):
# DATABASE_URL=postgresql://user:password@postgres:5432/saas

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
PORT=3001
BACKEND_PORT=3001
NODE_ENV=development

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------
# Comma-separated list of allowed origins
# Add your Vercel preview deployments and production domains
CORS_ORIGINS=http://localhost:3000,https://*.vercel.app

# -----------------------------------------------------------------------------
# Authentication
# -----------------------------------------------------------------------------
# JWT Secret (use strong random string in production, minimum 32 characters)
JWT_SECRET=your-jwt-secret-key

# Clerk Configuration (get from Clerk dashboard)
CLERK_SECRET_KEY=your-clerk-secret-key
CLERK_PUBLISHABLE_KEY=your-clerk-publishable-key

# -----------------------------------------------------------------------------
# API Security
# -----------------------------------------------------------------------------
API_KEY=your-api-key

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
# Options: debug, info, warn, error
LOG_LEVEL=debug

# =============================================================================
# PRODUCTION NOTES:
# =============================================================================
# For production deployment:
# 1. Use strong, randomly generated passwords (minimum 32 characters)
# 2. Never commit actual credentials to version control
# 3. Use environment-specific files (.env.production, .env.docker.prod)
# 4. Consider using secret management services (Azure Key Vault, etc.)
# 5. Set LOG_LEVEL=info or LOG_LEVEL=error in production
# =============================================================================
